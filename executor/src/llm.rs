// This module has been replaced by inference.rs.
// The app is now self-contained: the model runs directly inside the Rust binary
// via candle (https://github.com/huggingface/candle). No HTTP server is needed.
